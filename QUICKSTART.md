# 🚀 Быстрый старт - Metch

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка базы данных

### Вариант A: Использование Prisma Postgres (Рекомендуется для разработки)

```bash
npx prisma dev
```

Эта команда запустит локальный PostgreSQL сервер автоматически.

### Вариант B: Использование своей PostgreSQL базы данных

1. Создайте базу данных:
```sql
CREATE DATABASE metch;
```

2. Обновите `DATABASE_URL` в файле `.env`:
```env
DATABASE_URL="postgresql://username:password@localhost:5432/metch"
```

3. Выполните миграции:
```bash
npx prisma migrate dev --name init
```

## Шаг 3: Генерация Prisma Client

```bash
npx prisma generate
```

## Шаг 4: Запуск приложения

```bash
npm run dev
```

Откройте браузер и перейдите на http://localhost:3000

## 🎉 Готово!

Теперь вы можете:
1. Зарегистрироваться на сайте
2. Создать свой профиль
3. Просматривать анкеты других пользователей
4. Отправлять лайки и сообщения
5. Попробовать Premium функции (в демо режиме)

## 📊 Админ панель Prisma Studio

Для управления данными в базе данных:

```bash
npx prisma studio
```

Откроется GUI на http://localhost:5555

## 🐛 Решение проблем

### Ошибка с Node.js версией
Если вы видите предупреждение о версии Node.js, обновите до версии 20.9.0 или выше:

```bash
nvm install 20
nvm use 20
```

### Ошибка подключения к базе данных
Проверьте, что:
- PostgreSQL запущен
- DATABASE_URL в .env правильно настроен
- База данных создана

### Ошибка при генерации Prisma Client
Попробуйте:

```bash
rm -rf node_modules
npm install
npx prisma generate
```

## 📝 Тестовые данные

Для создания тестовых пользователей используйте страницу регистрации на http://localhost:3000/auth/signup

Рекомендуемые тестовые данные:
- Email: test@example.com
- Password: password123
- Name: Тест Пользователь
- Gender: Male/Female
- Birth Date: 1995-01-01
- Looking For: Companion

---

Приятного использования! ❤️
