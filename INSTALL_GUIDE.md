# 📖 Полная инструкция по установке и запуску

## ⚠️ Важно: Обновите Node.js!

Текущая версия: **Node.js 18.20.6**  
Требуется: **Node.js >= 20.9.0**

### Обновление Node.js:

**Вариант 1: Используя NVM (рекомендуется)**
```bash
# Установите NVM если ещё нет
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезапустите terminal

# Установите Node.js 20
nvm install 20
nvm use 20
nvm alias default 20

# Проверьте версию
node -v  # должно быть v20.x.x
```

**Вариант 2: Прямая установка**
- Скачайте с https://nodejs.org/ (LTS версия 20.x)
- Установите
- Перезапустите terminal

---

## 📦 Шаг 1: Зависимости

```bash
cd /Users/albertgiloan/Desktop/Metch/metch-app

# Установите все зависимости
npm install
```

---

## 🗄️ Шаг 2: База данных

### Вариант A: PostgreSQL локально

1. **Установите PostgreSQL:**
```bash
# macOS
brew install postgresql@16
brew services start postgresql@16

# Или скачайте с https://postgresapp.com/
```

2. **Создайте базу данных:**
```bash
# Подключитесь к PostgreSQL
psql postgres

# Создайте БД
CREATE DATABASE metch;

# Создайте пользователя (опционально)
CREATE USER metch_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE metch TO metch_user;

# Выйдите
\q
```

3. **Обновите .env:**
```env
DATABASE_URL="postgresql://username:password@localhost:5432/metch"

# Или если используете пользователя по умолчанию
DATABASE_URL="postgresql://postgres:@localhost:5432/metch"
```

### Вариант B: Облачная БД (Supabase, Neon, Railway)

1. **Создайте БД в облаке:**
   - [Supabase](https://supabase.com/) - бесплатно
   - [Neon](https://neon.tech/) - бесплатно
   - [Railway](https://railway.app/) - $5/месяц

2. **Скопируйте connection string в .env:**
```env
DATABASE_URL="postgresql://user:pass@host:5432/database?sslmode=require"
```

---

## 🔧 Шаг 3: Настройка Prisma

```bash
# Выполните миграции (создание таблиц)
npx prisma migrate dev --name init

# Сгенерируйте Prisma Client
npx prisma generate

# Опционально: откройте Prisma Studio для управления данными
npx prisma studio
```

---

## 📧 Шаг 4: Email (опционально)

Для отправки email уведомлений настройте SMTP в `.env`:

### Gmail:
1. Включите 2FA в аккаунте Google
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Добавьте в .env:

```env
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-app-password-here"
EMAIL_FROM="Metch <noreply@metch.com>"
```

### Другие провайдеры:
```env
# Mailgun
EMAIL_HOST="smtp.mailgun.org"
EMAIL_PORT="587"

# SendGrid
EMAIL_HOST="smtp.sendgrid.net"
EMAIL_PORT="587"

# Яндекс
EMAIL_HOST="smtp.yandex.ru"
EMAIL_PORT="465"
```

---

## 🚀 Шаг 5: Запуск

```bash
# Запустите dev сервер
npm run dev
```

Приложение будет доступно на: **http://localhost:3000**

---

## 🎯 Шаг 6: Первое использование

1. **Откройте браузер:** http://localhost:3000

2. **Зарегистрируйтесь:**
   - Перейдите на /auth/signup
   - Заполните форму регистрации
   - Если настроен email, получите приветственное письмо

3. **Заполните профиль:**
   - Перейдите в "Мой профиль"
   - Нажмите "Редактировать"
   - Загрузите аватар и фото
   - Заполните информацию о себе

4. **Начните знакомиться:**
   - Просматривайте анкеты на /profiles
   - Отправляйте лайки
   - Пишите сообщения (10 в день для free пользователей)
   - Пробуйте Premium функции

---

## 🧪 Шаг 7: Тестирование функций

### Real-time сообщения:
1. Откройте два окна браузера (или используйте инкогнито)
2. Зарегистрируйте двух пользователей
3. Отправьте сообщение
4. Увидите мгновенную доставку и индикатор "набирает"

### Видео-звонки:
1. Разрешите доступ к камере/микрофону
2. Перейдите на /video-call?userId=ID_ПОЛЬЗОВАТЕЛЯ
3. Нажмите "Начать звонок"
4. Второй пользователь получит входящий звонок

### Email уведомления:
1. Настройте SMTP
2. Выполните действия (лайк, сообщение, регистрация)
3. Проверьте email

---

## 📁 Создание папок

Создайте папку для загружаемых файлов:

```bash
mkdir -p public/uploads/photos
```

---

## 🐛 Решение проблем

### Ошибка "Node.js version"
```bash
# Обновите Node.js до версии >= 20.9.0
nvm install 20
nvm use 20
```

### Ошибка подключения к БД
```bash
# Проверьте, что PostgreSQL запущен
brew services list

# Или
ps aux | grep postgres

# Перезапустите если нужно
brew services restart postgresql@16
```

### Ошибка Prisma
```bash
# Пересоздайте клиент
rm -rf node_modules/.prisma
npx prisma generate
```

### Ошибка портов
```bash
# Если порт 3000 занят, используйте другой
PORT=3001 npm run dev
```

### Socket.io не подключается
```bash
# Проверьте в браузере консоль
# Убедитесь, что сервер запущен
# Проверьте, что путь /api/socket/io доступен
```

---

## 📊 Полезные команды

```bash
# Просмотр логов БД
npx prisma studio

# Проверка схемы Prisma
npx prisma validate

# Форматирование схемы
npx prisma format

# Сброс БД (удалит все данные!)
npx prisma migrate reset

# Сборка для продакшн
npm run build
npm start

# Линтер
npm run lint
```

---

## 🎨 Дополнительная настройка

### Изменение порта:
```env
# .env.local
PORT=3001
```

### Отключение email:
Просто не настраивайте `EMAIL_USER` в `.env` - функционал продолжит работать без email

### Отключение Socket.io:
Приложение работает и без Socket.io, но сообщения не будут real-time

---

## 📝 Checklist перед запуском:

- [ ] Node.js >= 20.9.0 установлен
- [ ] PostgreSQL запущен
- [ ] База данных создана
- [ ] DATABASE_URL настроен в .env
- [ ] npm install выполнен
- [ ] npx prisma migrate dev выполнен
- [ ] npx prisma generate выполнен
- [ ] (Опционально) Email настроен
- [ ] npm run dev запущен
- [ ] Браузер открыт на localhost:3000

---

## 🎉 Готово!

Если все шаги выполнены, приложение должно работать!

**Основные URL:**
- Главная: http://localhost:3000
- Регистрация: http://localhost:3000/auth/signup
- Вход: http://localhost:3000/auth/signin
- Профили: http://localhost:3000/profiles
- Сообщения: http://localhost:3000/messages
- Мой профиль: http://localhost:3000/profile/me
- Premium: http://localhost:3000/premium

---

**Удачного знакомства! ❤️**
